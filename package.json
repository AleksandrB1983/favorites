{
  "name": "cypress1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",  
    "chrome": "cypress run --browser chrome --headed --config-file cypress.config.js",
    "chrome": "cypress run --browser chrome --headless --config-file cypress.config.js",
    "edge": "cypress run --browser edge --headed --config-file cypress.config.js",
    "edge": "cypress run --browser edge --headless --config-file cypress.config.js",
    "electron": "cypress run --browser electron --headed --config-file cypress.config.js",
    "electron": "cypress run --browser electron --headless --config-file cypress.config.js"    
  },  

  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.12.0"
  }
}
